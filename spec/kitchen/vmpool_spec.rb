require "spec_helper"

RSpec.describe Kitchen::Vmpool do
  it "has a version number" do
    expect(Kitchen::Vmpool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
